| Database | Best for |
| --- | --- |
| SQLite | a single process on one machine, with no server to run and backups that are a file copy |
| PostgreSQL | a growing product that needs strict typing, rich indexes, concurrent writes and extensions |

Start with SQLite and move when you add a second server.
